# Project images

Store images in `projects/<project-slug>/` and reference them from
`src/content/projects.json` with absolute paths beginning `/images/projects/`.

Prefer AVIF or WebP, reserve stable image dimensions and provide meaningful alt
text for every content-bearing image.
